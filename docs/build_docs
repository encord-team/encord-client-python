#!/usr/bin/env bash

set -e

echo "Isort"
# isort ./**/*.py --profile black --line-length 60
poetry run isort . --resolve-all-configs

echo "Black"
poetry run black . --include ".*\.py" --line-length 80

poetry run python -c "from PIL import Image; print(Image)"

poetry run make html
